#!/usr/bin/env bash
# Runs celery cron scheduler.
# Assumes bin/install has been run.

celery_app="app.task.registry"
celery_schedule="./tmp/celerybeat-schedule"

# Echo commands
set -x

newrelic-admin run-program celery beat -A $celery_app -s $celery_schedule $*
